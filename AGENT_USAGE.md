# AGENT_USAGE.md

## 1. 코딩 에이전트에게 맡긴 부분

- 백엔드 인증/세션 관리 파이프라인(broadcastFetcher.js: 로그인, 로테이팅 쿠키 처리, 카테고리 매핑, 폴링/캐싱) 실제 구현
- Express 라우터(routes/broadcasts.js)와 app.js 연결, CORS 설정
- 프론트엔드 4개 조각(useBroadcasts 훅, TypeToggle, BroadcastTable, App.jsx 통합) 실제 코드 작성
- README.md 초안 작성

## 2. 직접 판단하거나 직접 구현한 부분

- 브라우저 개발자도구 Network 탭으로 실제 API를 직접 분석해 인증 방식(쿠키 기반, 매 응답마다 로테이션), 라이브/홈쇼핑 엔드포인트 및 필드 구조 파악
- 라이브(list)/홈쇼핑(list_hs) 응답 필드명이 서로 달라 통일 스키마(title/category/startTime 등) 설계
- realtime_ranking API가 테이블과 무관한 별도 위젯임을 직접 확인해 설계에서 제외
- 401 재시도 정책(즉시 재로그인 대신 연속 2회 이상일 때만 재로그인) 결정
- 계정 정보를 에이전트 채팅에 노출하지 않고 .env에 직접 작성하는 방식 유지

## 3. 에이전트 결과를 고친 지점

- broadcastFetcher.js 초기 구현이 실제 API 필드명(datetime_start, sales_amt 등)을 추정치로 작성해 값이 undefined로 나와, 직접 캡처한 실제 응답 JSON을 기준으로 필드명을 수정하도록 재지시
- 401 응답 시 매번 즉시 재로그인하도록 짜여 있어 세션을 낭비하는 문제가 있었고, 연속 2회 이상일 때만 재로그인하도록 로직을 변경
- 라이브/홈쇼핑 방송시간 필드 길이가 다른 것(10자리 vs 12자리)을 미리 확인해 파싱 함수가 두 포맷을 분기 처리하도록 지시

## 4. 아직 완전히 이해하지 못한 부분

- 로그 파일(logs/)이 세션 도중 작업 디렉터리(cwd) 변경에 따라 logs/, server/logs/ 두 곳에 나뉘어 기록되는 내부 동작을 결과만으로 유추한 상태(추후에 문제가 될까봐 일단은 방치해 뒀습니다.)